{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/server" }
  ],
  "functions": {
    "api/server.ts": {
      "maxDuration": 300
    }
  }
}
